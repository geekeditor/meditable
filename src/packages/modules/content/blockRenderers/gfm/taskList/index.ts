import { MEBlockType } from "@/packages/types";
import MEBlockRenderer from "../../renderer";

export default class METaskListRenderer extends MEBlockRenderer {
    static type: MEBlockType = "task-list";
}